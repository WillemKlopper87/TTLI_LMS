# Backlog 2026-09-11 — code-review remediation queue

**Status refreshed:** 2026-09-15.

**Purpose:** ordered execution queue for the verified 2026-09-11 review findings plus the
production-hardening gate. [`BACKLOG.md`](BACKLOG.md) remains the status authority; this file
carries sequencing and exit criteria. Sprint-1 merge/evidence coordination is tracked in
GitHub issue #31.

**Governing rule:** harden and simplify what exists. Do not start Assessment Platform,
Programmes, Analyst Workspace, Partner Portal, Licensing+xAPI, or Phase 6 AI implementation
until Sprint 1 is complete or the platform owner explicitly accepts a named residual risk.

**Required merge gate:** short-lived branch → PR → `quality`, `web`, `authenticated-e2e`,
`secrets`, and `images` green → merge. Browser/API changes must add evidence at the named risk
seam; operator/legal evidence remains explicit rather than being represented as completed code.

---

## Sprint 0 — process baseline

| | Item | Evidence / exit criterion |
|---|---|---|
| [x] | #22 — scoped, expiring ClamAV libxml2 disposition | Merged; required CI green. |
| [x] | Protect `main`: PR required; five required checks; up-to-date; linear history; no force-push/deletion | Protection established before the UAT baseline. |
| [x] | Current-state `NEXT_AGENT_BRIEF.md`; archive superseded narrative | Merged with #22. |
| [x] | Merge #22 | `main` advanced through protected PR flow. |
| [x] | Tag `v0.9.0-uat1` | Annotated tag exists and points at `f9682d6975ce3c304f121b835079d6fe30796446`. |
| [ ] | Capture explicit direct-push rejection evidence | Attempt a harmless throwaway direct push to protected `main`; record the refusal in the authoritative backlog if/when performed. |

---

## Sprint 1 — production safety

Follow GitHub issue #31's order. A later tranche may be prepared separately, but it must not be
merged ahead of the active gate.

### 1a. Quick correctness wins

| | Ref | Item | Current evidence / exit criterion |
|---|---|---|---|
| [x] | **F1** | Four authenticated browser specs were outside the API-backed required loop and could skip silently. | **#29 merged 2026-09-15.** CI #178 log proves all nine named specs execute under `REQUIRE_API_E2E=1`; no silent skips. |
| [x] | **F2** | Tenant-domain cache was not invalidated on add/remove. | **#29 merged 2026-09-15.** Domain mutation uses `cache-delete → commit → cache-delete`; regression tests cover normalization and add/delete hit/miss eviction. |
| [ ] | **F9** | Worker health/deploy gate accepts a running-but-broken worker. | **Owned by #25.** Refresh onto current `main`, require all five CI gates, merge active arq canary/health and release evidence code, then prove a deliberately broken worker fails release. |

### 1b. T10 — deployment integrity and rollback proof

| | Step | Current owner / exit criterion |
|---|---|---|
| [ ] | Active worker canary/health replaces process-alive acceptance | **#25 code tranche.** A worker that boots but cannot consume work must fail the release inside the documented timeout. |
| [ ] | Release evidence manifest on every deploy | **#25 code tranche.** Record API readiness, worker heartbeat/canary, web HTTP, image IDs/digests, app version, Git SHA and timestamp; persist with operational evidence. |
| [ ] | Forced rollback rehearsal | **Operator evidence after #25 merges.** Deploy a deliberately broken worker, observe canary failure, roll API+worker back as one unit, and record before/after manifests plus known-good canary. |

### 1c. T11 — minimum viable observability

**Owner:** #30. Sentry remains for error capture; this tranche adds aggregate operational
visibility. Keep metric labels free of tenant/user IDs, raw object keys, exception text and
other high-cardinality or personal data.

| | Layer | Minimum signals / exit criterion |
|---|---|---|
| [ ] | API | Request count/rate, latency distribution including p95, 4xx/5xx; DB pool pressure. Metrics are route-template labelled, not raw URL labelled. |
| [ ] | Worker / queue | Queue depth; completed/failed/retried jobs; duration/heartbeat or equivalent liveness signal. |
| [ ] | Database | Pool size/in-use/overflow plus server connection saturation and database size where available. |
| [ ] | Storage | Success/failure counters at the shared storage boundary without object-key labels. |
| [ ] | Structured logs | JSON logs with request/job correlation; retained/searchable without SSH and surviving container restart. |
| [ ] | Alerts + dashboard | Documented thresholds from `06_OPERATIONS.md`, internal-only collection, one operator dashboard, and runbook/action context. |

### 1d. T9 — restore rehearsal evidence

Code is not the missing part. Only real operator evidence closes this item.

| | Step | Owner | Exit criterion |
|---|---|---|---|
| [ ] | Supply real encrypted off-VM `rclone` destination and accountable owner | Platform/operator | Production-shaped host has real values, not placeholders. |
| [ ] | Run `scripts/backup-production.sh` | Agent + operator | Archive is visible at the off-host destination and age is within policy. |
| [ ] | Run `scripts/restore-drill.sh` in isolation | Agent + operator | DB + all required object-storage buckets restore; application boots; representative learner flow succeeds; measured RPO/RTO recorded. |

### 1e. T12 — POPIA operational lifecycle

**Owner for current API tranche:** #26. Do not mark the entire lifecycle done merely because the
API tranche merges; retention policy, operator/admin workflow and legal/owner responsibilities
remain separate closure conditions.

| | Step | Current owner / exit criterion |
|---|---|---|
| [ ] | Privacy request model/state, data export/access path | **#26 code tranche.** Migration/RLS/grants/tests clean; export remains tenant/subject scoped. |
| [ ] | Erasure/anonymisation with financial/accreditation exceptions | **#26 code tranche.** Retained invoices/ledger/certificates/audit records survive where required but are de-identified appropriately. |
| [ ] | Legal hold enforcement | **#26 code tranche.** Erasure is refused/limited with an auditable reason when a hold applies. |
| [ ] | Consent withdrawal / marketing suppression | **#26 code tranche.** Withdrawal has an auditable durable effect. |
| [ ] | Retention policy and scheduled enforcement | Remaining T12 work after the API tranche unless #26 expands to include it. Policy periods and owner/legal basis must be recorded first. |
| [ ] | Admin/operator privacy queue and ageing | Remaining operational/UI tranche unless #26 explicitly includes it; must be accessible and axe-clean. |
| [ ] | Information Officer / legal responsibility | **Owner decision/evidence.** Confirm B8 rather than coding around it. |

### 1f. Integration acceptance

| | Item | Blocker | Exit criterion |
|---|---|---|---|
| [ ] | Payfast sandbox matrix | B4 merchant credentials | Success, cancel, duplicate/replayed ITN, bad signature, wrong amount/currency, delayed ITN, refund and gateway-outage behaviour recorded. |
| [ ] | Real Entra/OIDC acceptance, if pilot uses SSO | Tenant app registration | Real IdP login/callback/session accepted against the pilot tenant. |
| [ ] | Production-shaped load test | Production-shaped host | Pilot profile exercised; p95/throughput, DB-pool saturation and duplicate-fulfilment assertions recorded. |

---

## Sprint 2 — structural debt after Sprint 1

Profile before optimising. Do not let this queue displace the remaining production evidence.

### 2a. Performance and data access

| | Ref | Item | Exit criterion |
|---|---|---|---|
| [ ] | **F3** | Gradebook N+1: repeated quiz + attempts fetches per block. | Batched query shape; query count asserted. |
| [ ] | **F4** | Learner dashboard N+1: repeated quiz/attempt-count work per lesson/enrolment. | Grouped/batched query; query count asserted. |
| [ ] | **F5** | Campaign send loops contacts and enqueues inside request transaction. | Request enqueues bounded background work and returns promptly; job path covered. |
| [ ] | **F6** | Admin lists/exports lack consistent pagination/ceilings. | Stable cursor/limit/max-size policy across named endpoints; oversized exports become background jobs where appropriate. |
| [ ] | O12-adjacent | Capture slow reporting/catalogue/finance queries. | `EXPLAIN (ANALYZE, BUFFERS)` evidence attached before any index is added. |
| [ ] | **F18** | Anonymous storefront pages force dynamic/no-store and lack loading states. | Appropriate caching/revalidation + route loading states without stale tenant/public data. |

### 2b. Frontend contract safety

| | Step | Exit criterion |
|---|---|---|
| [ ] | Build typed domain facade on committed `packages/api-client` schemas | Facade has unit coverage and contains no parallel hand-written response contracts. |
| [ ] | Migrate highest-risk pages first: checkout → learning/player → assessments → organisation seats → finance → workshops → admin | Local duplicate interfaces disappear page by page; generated-client drift gate remains green. |
| [ ] | Add review/lint guard against new raw high-risk API contracts | New violations fail review/CI rather than accumulating. |

### 2c. Component layer, decomposition and formatting

| | Ref | Item | Exit criterion |
|---|---|---|---|
| [ ] | O8/O14 | Establish a small shared UI component layer and split large mixed-responsibility pages/services only after characterization tests. | Reused components replace duplication; decomposition changes behaviour zero or intentionally with tests. |
| [ ] | **F13** | Raw `toLocaleDateString()` and local ZAR formatting bypass shared formatters. | One shared date/money convention across the app. |
| [ ] | **F20** | Broad exceptions in tenant-user role/suspension paths. | Specific exception handling or explicit justification + tests. |

### 2d. UX quick wins from screenshot pass

| | Ref | Item | Exit criterion |
|---|---|---|---|
| [ ] | **F17a** | Push prompt appears on inappropriate authenticated routes. | One-time/dismissible; excluded from checkout/player. |
| [ ] | **F17b** | Public header overflows on mobile. | Mobile navigation at narrow width; axe-clean. |
| [ ] | **F17c** | Mobile catalogue places full filter sidebar ahead of results. | Collapsed/mobile filter interaction. |
| [ ] | **F17d** | Player next-lesson control can sit partly off-screen. | Control remains inside usable viewport/content column. |
| [ ] | **F17e** | Learner navigation is admin-heavy; dashboard lacks dominant continue CTA. | Learner IA simplified; in-progress course/action is primary. |

### 2e. Test, CI and deployment hygiene

| | Ref | Item | Exit criterion |
|---|---|---|---|
| [ ] | **F7** | Stale `docker-compose.prod.yml` diverges from single-VM topology. | Remove it or derive both from one maintained source; config preflight stays green. |
| [ ] | **F8** | Resource limits/log rotation incomplete. | Explicit service limits and bounded container log retention. |
| [ ] | **F10** | Dependabot lacks Docker ecosystem coverage. | Docker dependency update coverage for app/infra image roots. |
| [ ] | **F11** | CI repeats browser install/build/test/tool work. | Remove measurable duplication without weakening independent gates/evidence. |
| [ ] | **F12** | Package/transitive resolution undermines image reproducibility. | Lock/hash strategy makes the install reproducible enough for the documented threat model. |
| [ ] | **F19** | Local `ttli_test` state accumulates across runs. | Clean/session-isolated database strategy; reruns do not inherit state. |
| [ ] | **F20** | Thin tests around audit/invoices/question bank/tenant branding/invoicing. | Focused tests at behavioural seams, not raw coverage count. |
| [ ] | **F21** | Migration round-trip exercises only the newest migration. | Periodic full downgrade/upgrade path or equivalent defensible migration-history verification. |
| [ ] | **F22** | Trivy installed from mutable apt channel. | Checksum/signature-pinned release install comparable to other security tooling. |
| [ ] | — | Media lifecycle browser/API journey. | Upload → scan → finalise → transcode → attach/publish → enrol/play + expiry/range semantics. |
| [ ] | — | BFF edge tests. | Binary/stream/range/rate-limit/correlation/header-forwarding semantics pinned. |
| [ ] | — | Flake reduction / platform-neutral dev scripts. | Behavioural assertions replace event-loop-tick timing; platform detection replaces hard-coded Windows-only paths where practical. |

### 2f. Documentation hygiene

| | Item | Exit criterion |
|---|---|---|
| [ ] | Keep `README.md` as concise product/architecture/bootstrap/release entry point. | Current, navigable, not a status dump. |
| [ ] | Record durable owner/architecture decisions as ADR/decision artifacts. | One decision per file with owner/date/rationale. |
| [ ] | Keep current truth in `BACKLOG.md`; move historical narrative to `docs/archive/`. | No contradictory active-status documents. |

---

## Sprint 3 — product work, only after hardening

| | Item | Ref |
|---|---|---|
| [ ] | Owner-approved remaining product gaps: departments, certificate design, CRM depth, search/notifications/preferences, workshops calendar, cohort lifecycle, gradebook UI/moderation/export, competency framework | P8, P10, P12, P15, P16, P17a, P18, P19 |
| [ ] | Bulk/zip content import | R7 |
| [ ] | Staged rollout/feature-flag framework before AI rollout | R9 |
| [ ] | Interoperability strategy decision: xAPI/LTI/HRIS/history import/completion webhooks | R15 |
| [ ] | Phase 6 AI vertical slice, inert behind tenant kill switch | T13 / P11 |

Explicitly declined/deferred items from the historical backlog remain archived and do not become
build work merely because this file is shorter.

---

## Do not

- Do not weaken Trivy, coverage, drift, RLS or authenticated-E2E gates to make a branch green.
- Do not merge a later Sprint-1 tranche ahead of the active #31 step.
- Do not convert operator/legal evidence into a checkbox merely because supporting code exists.
- Do not create another status document. Update `BACKLOG.md` and this queue.
- Do not push directly to `main` or rewrite published history.
- Do not start Phase 6 or the five future product-module implementations before Sprint 1 closes.

## Owner/operator asks that unblock Sprint 1

Real encrypted off-VM backup destination + accountable owner (T9) · Payfast sandbox credentials
(B4) · production-shaped host (T9/load test) · Information Officer/legal responsibility (T12/B8)
· Entra app registration if the pilot uses SSO · signed AI data-residency decision before T13.
