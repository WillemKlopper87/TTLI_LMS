# Backlog 2026-09-11 — code-review remediation queue

**Purpose:** the ordered execution queue for the next agent(s), covering every item from the 2026-09-11 code review (mine and the independent one). This is a *dated, ordered view* of
[`BACKLOG.md`](BACKLOG.md) — every item below cites a BACKLOG / ledger ID; BACKLOG stays the
authority on status. Tick boxes here as you go, and update the BACKLOG row in the same commit.

**Governing rule for this sprint:** harden and simplify what exists. No new product features
until Sprint 1 is complete. Phase 6 AI stays gated behind T13.

**Process (non-negotiable since 2026-09-11):** `main` is protected. Every change is a
short-lived branch → PR → squash merge, with `quality`, `web`, `authenticated-e2e`, `secrets`
and `images` green. Conventional commit prefixes (`fix(payments):`, `ops(release):`,
`refactor(web):`). Tag every UAT/pilot hand-off.

**Per-item definition of done:** code + test at the seam + BACKLOG row updated + live smoke
through the BFF at :3010 where a screen is involved. Evidence (a run URL, a screenshot, a
timing) goes in the BACKLOG row, not in a new status document.

---

## Sprint 0 — Stabilise the process (P0) · ~½ day

| | Item | Ref | Exit criterion |
|---|---|---|---|
| [x] | Scoped, expiring Trivy exception for CVE-2026-74860 (ClamAV, libxml2 Python bindings absent) | PR #22 | `quality` green on the PR |
| [x] | Branch protection on `main`: PR required, 5 required checks, up-to-date, linear history | — | `gh api .../branches/main/protection` returns the rule |
| [x] | `NEXT_AGENT_BRIEF.md` is current-state only; 2026-09-08 version archived | PR #22 | `docs/check_links.py` passes |
| [ ] | Merge #22 via squash; delete the branch | — | `main` green |
| [ ] | Tag `v0.9.0-uat1` on the merge commit; add a `[0.9.0-uat1]` heading to `CHANGELOG.md` | O5 | `git tag` shows it; the UAT team can name the build they tested |
| [ ] | Prove the gate: attempt `git push origin main` from a throwaway commit — it must be refused | — | Refusal message captured in the BACKLOG O5 row |

---

## Sprint 1 — Production safety (P1) · ~3 weeks

Order matters: F1/F2 are one-day correctness wins hidden by a green build; T10/T11 make T9's
rehearsal observable; T12 is the largest and least dependent, so it can run in parallel.

### 1a. Quick correctness wins (do first, ~1 day total)

| | Item | Ref | Where | Exit criterion |
|---|---|---|---|---|
| [ ] | Four Playwright specs never execute in CI (`admin`, `learner`, `session-refresh`, `survey-pairing` skip without an API; only 5 specs are in the `authenticated-e2e` loop) | **F1** | `.github/workflows/ci.yml:757-761`; add the `REQUIRE_API_E2E` hard-fail block the other five carry | All 9 authenticated specs run and pass in the `authenticated-e2e` job |
| [ ] | Tenant-domain cache never invalidated: 60 s `tenant:host:*` cache, but `POST/DELETE /tenant/domains` never deletes the key — a removed domain keeps resolving to the old tenant | **F2** | `core/tenancy.py:26,64`; `routers/tenant.py` domain create/delete | Test: delete a domain → next request on that host is refused immediately |
| [ ] | `worker` has no compose healthcheck; the rolling updater only polls "running" | **F9** | `infra/docker-compose.single-vm.yml:233-249`; `scripts/rolling-update.sh:138` | Healthcheck reads arq's `arq:health-check` Redis key; a worker that boots but cannot reach Redis fails the deploy |

### 1b. T10 — Deployment integrity and rollback proof (M)

| | Step | Exit criterion |
|---|---|---|
| [ ] | Active worker canary: `arq --check src.workers.main.WorkerSettings` (or the health-key read from F9) replaces the process-alive check | A stalled worker fails the release within 60 s |
| [ ] | Release evidence manifest written on every deploy: API `/health/ready`, worker heartbeat, web HTTP, running image IDs, registry digests, `APP_VERSION`, Git SHA, timestamp — persisted off-host with the backups | `scripts/rolling-update.sh` emits `release-<sha>.json`; the file is what BACKLOG T10 links to |
| [ ] | Forced rollback rehearsal: deploy a deliberately broken worker image → canary fails → API + worker roll back as one unit → known-good canary passes | Rehearsal run recorded (manifest before/after) in BACKLOG T10 |

### 1c. T11 — Minimum viable observability (M)

Sentry stays for errors. This adds the aggregate view a money-handling LMS cannot run without.

| | Layer | Minimum signals | Exit criterion |
|---|---|---|---|
| [ ] | `/internal/health/dependencies` (admin-only; keep `/health/ready` as a fast `SELECT 1`) | database, redis, object_storage, clamav, mail, worker → `ok/degraded/down` | Endpoint + test; the updater and the monitor call it |
| [ ] | API metrics | req/s, p50/p95/p99 by route, 4xx/5xx rate, DB pool in-use/waiting | Prometheus-style `/metrics` on the API, scraped |
| [ ] | Worker metrics | queue depth, oldest job age, completed/failed/retried, transcode failures, heartbeat age | Exported from the arq process |
| [ ] | Business signals | Payfast ITN failures, EFT approvals > 48 h, certificate-issue failures, email send failures, transcode failures, enrolment-fulfilment failures | Each is a counter with an alert rule |
| [ ] | Infra | CPU/RAM/disk, Postgres connections + disk, Redis memory, Garage health, TLS expiry, backup age, last-restore-test age | One operator dashboard; every alert links a runbook section in `06_OPERATIONS.md` |
| [ ] | Structured log shipping off the VM (JSON, with `x-request-id`) | Logs survive a container restart and are searchable by request id |

### 1d. T9 — Restore rehearsal evidence (M, **needs a human**)

Code is done. What is missing is evidence, and only an operator with the real off-VM crypt
destination can produce it.

| | Step | Owner | Exit criterion |
|---|---|---|---|
| [ ] | Owner supplies the `rclone` crypt remote + an accountable owner name (BACKLOG B-list) | Human | Values in `.env.prod` on the production-shaped host |
| [ ] | Run `scripts/backup-production.sh` on that host; confirm the archive lands off-host | Agent + human | Archive listed at the remote |
| [ ] | Run `scripts/restore-drill.sh` in isolation: DB + all five buckets → app boots → a seeded learner logs in, opens a lesson, plays media, downloads a certificate | Agent + human | Measured RPO and RTO recorded in BACKLOG T9; both inside the 15 min / 4–8 h targets |

### 1e. T12 — POPIA operational lifecycle (M–L)

Build it as a first-class domain, not a delete button.

| | Step | Exit criterion |
|---|---|---|
| [ ] | `services/privacy/` — `export.py`, `correction.py`, `erasure.py`, `retention.py`, `legal_hold.py`, `consent.py` | Each has a unit tier and an RLS-aware integration test |
| [ ] | `PrivacyRequest` model: type, subject, received_at, due_at (30 days per POPIA), state, assigned_to, verification_state, legal_hold, completed_at, audit_reference — migration `0046` | `alembic check` clean; RLS policy + grants tested |
| [ ] | Erasure = delete where permitted, **anonymise where financial/accreditation retention applies** (invoices, ledger, certificates, audit events are retained and de-identified, never deleted), restrict under legal hold, and record the reason per record class | Test proves an erased learner's invoice still exists with a tokenised name |
| [ ] | Export-my-data: JSON + attachments (certificates, invoices) as a background job with a signed download | Playwright journey from `/account` |
| [ ] | Consent withdrawal feeds the marketing suppression list; retention job runs nightly | Audit event per action |
| [ ] | Admin UI: `/admin/privacy` queue with due-date ageing | axe-clean |

### 1f. Integration acceptance (needs owner credentials)

| | Item | Ref | Blocked on |
|---|---|---|---|
| [ ] | Payfast sandbox acceptance suite: success, cancel, duplicate ITN, replayed ITN, bad signature, wrong amount/currency, delayed ITN, refund, gateway outage | B4 | Merchant credentials |
| [ ] | OIDC against a real Entra tenant (only if a pilot tenant uses SSO) | P4 | Tenant app registration |
| [ ] | Load test to the pilot profile: 100 concurrent authenticated, 50 media viewers, 10 checkouts/min; API p95 < 500–750 ms, zero duplicate fulfilment, no pool saturation | O12 | A production-shaped host (T9's) |

---

## Sprint 2 — Structural debt (P2) · ~4 weeks

### 2a. Performance and data-access (evidence-driven; profile before adding indexes)

| | Item | Ref | Where | Exit criterion |
|---|---|---|---|---|
| [ ] | Gradebook N+1: `session.get(Quiz)` + attempts query per block | **F3** | `services/gradebook.py:211-231` | One `IN` fetch + one `DISTINCT ON` latest-attempt query; query count asserted in a test |
| [ ] | Learner dashboard N+1: per enrolment → lesson → block `session.get(Quiz)` + `_attempts_remaining` COUNT | **F4** | `services/dashboard.py:262-285,144-154` | Grouped query before the loop; query count asserted |
| [ ] | Campaign send loops every contact inside the request transaction | **F5** | `services/campaigns.py:171-230`, `routers/campaigns.py:148-168` | `POST /campaigns/{id}/send` enqueues one arq job and returns 202 |
| [ ] | Pagination ceilings on every admin list/export: `GET /video-assets`, `/tenant/users`, audit, learners, org progress, finance, campaigns, certificates, enrolments | **F6** | e.g. `routers/media.py:129-150`; copy `routers/deals.py:74-88` | `limit`/cursor/stable order/max page size everywhere; exports > N rows become background jobs |
| [ ] | Capture slow queries (reporting, org progress, learning-path rollups, catalogue facets, finance) and `EXPLAIN (ANALYZE, BUFFERS)` them; add only the indexes the plans justify | O12-adjacent | — | Plans attached to the BACKLOG row |
| [ ] | Storefront caching: drop `force-dynamic` + `no-store` on `/`, `/catalogue`, `/courses/[id]`, `/paths`, `/podcasts` in favour of `revalidate: 60`; add `loading.tsx` for the storefront group | **F18** | `app/page.tsx:71` etc., `lib/server-api.ts:30,63` | Anonymous pages served from cache; skeleton on first paint |

### 2b. Frontend contract safety (`REMEDIATION_LEDGER.md` M7 — the highest-value refactor)

| | Step | Exit criterion |
|---|---|---|
| [ ] | Typed facade `apps/web/lib/api/{auth,learning,commerce,organisations,workshops,reporting,admin}.ts` built on `packages/api-client` schemas | Facade has vitest coverage |
| [ ] | Migrate page by page, highest risk first: checkout → learning/player → assessments → organisation seats → finance → workshops → admin | Each page's local `interface` deleted; no `any` |
| [ ] | ESLint rule: pages may not import `authedFetch` directly | Lint fails on a new violation |

### 2c. Component layer and page decomposition (`BACKLOG.md` O8/O14)

| | Step | Exit criterion |
|---|---|---|
| [ ] | `components/ui/`: Button, Card, FormField, Select, Modal, Drawer, Table, Pagination, StatusBadge, EmptyState, LoadingState, ErrorState — Tailwind classes + tenant CSS variables, **no inline `style={{}}`** | Each with a component test |
| [ ] | `components/domain/`: CourseCard, LearnerProgress, PaymentStatus, AssessmentResult, WorkshopSession | Used by ≥ 2 pages each |
| [ ] | Split `lesson-activity-panel.tsx` (1,214 lines) by behaviour: VideoActivity, QuizActivity, SurveyActivity, AssignmentActivity, DownloadActivity, LessonProgress, ActivityNavigation | No file > 400 lines; existing Playwright journeys unchanged |
| [ ] | Same for `admin/workshops/page.tsx` (1,089), `admin/analytics/page.tsx` (630), `admin/paths/[pathId]/edit/page.tsx` (593), `checkout/page.tsx` (581) | — |
| [ ] | Once inline styles are gone, drop `'unsafe-inline'` from `style-src` in `proxy.ts` | CSP report-only run shows zero violations, then enforce |
| [ ] | Replace raw `toLocaleDateString()` (transcript, subscription, leads, credentials) and the local ZAR formatter in `admin/analytics` with `lib/format.ts` | **F13** | One date/money format across the app |

### 2d. UX quick wins (each ≤ ½ day; seen in the 2026-09-11 screenshot pass)

| | Item | Ref | Exit criterion |
|---|---|---|---|
| [ ] | Push-notification banner shows on every authenticated page including checkout and the player | **F17a** | Shown once, dismissal persisted, never on `/checkout` or `/learn/[id]` |
| [ ] | Public header overflows on mobile (nav items clipped, no hamburger — the admin shell has one) | **F17b** | Hamburger < `md`; axe-clean at 390 px |
| [ ] | Mobile catalogue renders the full filter sidebar above the results (~1,700 px before the first course) | **F17c** | Collapsed "Filters" button < `md` |
| [ ] | Player "Next lesson" floats half off-screen at 1440×900 | **F17d** | Pinned inside the content column |
| [ ] | Learner nav has 8 top-level items (an admin's IA); no "Continue" hero on the dashboard when a course is in progress | **F17e** | Subscription/Invoices under an account menu; in-progress course is the dominant CTA |

### 2e. Backend decomposition (`BACKLOG.md` O14; repeat the `workshops/` split pattern)

| | Module | Split into | Exit criterion |
|---|---|---|---|
| [ ] | `routers/auth.py` (683) | `auth/{passwords,login,mfa,magic_links,recovery,sessions}.py` | Router = validation → use-case → response; zero test changes |
| [ ] | `services/enrolment.py` (1,280) | `enrolment/{progress,completion,certificates,paths_hook}.py` | Characterise with tests first |
| [ ] | `routers/assessment.py` (1,091), `services/learning_paths.py` (931), `services/orders.py` (899) | By use case | Same |
| [ ] | Narrow the two uncommented `except Exception` in `services/tenant_users.py:162,174` | **F20** | Specific exception or a justification comment |

### 2f. Test and CI hygiene

| | Item | Ref | Exit criterion |
|---|---|---|---|
| [ ] | `ttli_test` is never reset — fixtures commit and state accumulates across local runs | **F19** | Drop-and-recreate at session start, or savepoint-rollback per test |
| [ ] | Media lifecycle journey: upload → scan → finalise → transcode → select → attach → publish → enrol → play → expiry/range | M2 | One Playwright spec + HLS/progressive API tests |
| [ ] | BFF edge tests: binary bodies, streamed responses, range headers, rate-limit headers, correlation id, approved request-header forwarding | M8 | vitest over `app/api/bff/[...path]/route.ts` |
| [ ] | Thin areas: `audit_read.py`, `invoice_documents.py` (assert PDF bytes/metadata), `question_bank.py`, `tenant_branding` (7 endpoints, 3 assertions), `invoicing.py` (0 refs) | **F20** | Per-module tests |
| [ ] | `docker-compose.prod.yml` is stale (0 healthchecks, ~15 env keys missing vs single-vm) | **F7** | Delete it, or derive both files from one `x-api-env` anchor |
| [ ] | No `mem_limit`/`cpus`/log rotation in any compose file | **F8** | `logging: json-file max-size 50m max-file 5` + limits per service in single-vm |
| [ ] | Dependabot has no `docker` ecosystem; base-image digests are bumped by hand | **F10** | Entries for `/apps/api`, `/apps/web`, `/infra`, `/infra/postfix-relay` |
| [ ] | CI repeats work: Playwright browsers installed twice uncached, `npm run build` ×3, full pytest then fast tier again | **F11** | `~/.cache/ms-playwright` cached; `build-push-action` with `cache-from: type=gha`; one `--cov` run |
| [ ] | Unpinned pip transitives make the digest-pinned image non-reproducible | **F12** | Hash-locked `requirements.lock`, `pip install --require-hashes` |
| [ ] | Trivy installed from a mutable apt channel while everything else is pinned | **F22** | Checksum-verified release download, as gitleaks already does |
| [ ] | Migration round-trip only exercises the newest migration | **F21** | Weekly `alembic downgrade base && upgrade head` |
| [ ] | `test_sso.py:645-669` counts event-loop ticks (flaky on a saturated runner); `retries: 1` hides Playwright flakes | — | Assert the executor was used; `retries: 0` + flaky count published |
| [ ] | `gates.sh`/`dev-up.sh` hard-code Windows venv paths and `netstat`/`taskkill` | — | Detect `bin` vs `Scripts`; `lsof` fallback |

### 2g. Documentation

| | Item | Exit criterion |
|---|---|---|
| [ ] | `README.md`: what TTLI is, architecture, bootstrap, current release status — nothing else | < 200 lines |
| [ ] | `docs/decisions/` for ADRs and owner decisions (start with the ten `01_PRD.md §1.4` items as they close) | One file per decision |
| [ ] | Purge stale narrative from `BACKLOG.md` "Suggested order" and P-rows already marked DONE (fold into the table only) | BACKLOG is only actionable work |
| [ ] | Rule enforced by review: a document describes current truth **or** historical truth, never both | — |

---

## Sprint 3 — Product (P3) · only after Sprints 1–2

| | Item | Ref |
|---|---|---|
| [ ] | Owner-approved product gaps that remain: departments (P8), certificate design (P10), CRM depth (P12), search + notification inbox (P15), workshops calendar (P16), gradebook UI (P18), cohorts (P17a) | BACKLOG P-list |
| [ ] | arq → SAQ/Streaq migration (redis-py < 6 pin; maintenance-only upstream) — before background load grows | R-list |
| [ ] | T13 → Phase 6 AI vertical slice, inert behind the tenant kill switch, only after the residency decision | P11 |

---

## Do not

- Do not add features, endpoints or screens not listed above without a BACKLOG row and an owner reason.
- Do not weaken Trivy, coverage or drift gates to get green. Fix, rebuild, or add a scoped expiring exception with digest-verified evidence.
- Do not rewrite Git history. Do not push to `main`.
- Do not start Phase 6 / any LLM call path.
- Do not create a new status document. BACKLOG rows carry status; this file carries order.

## Owner asks that unblock this sprint

`rclone` crypt destination + accountable owner (T9) · Payfast sandbox credentials (1f) · a production-shaped host (T9, load test) · Information Officer name (T12) · the AI data-residency decision (T13).
