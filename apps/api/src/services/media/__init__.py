"""The VOD transcode pipeline, ported from the in-house Streaming_Server
project's ordinary-HLS-origin path (06 §3). `Streaming_Server` itself is
never modified — see that section for full provenance.
"""
